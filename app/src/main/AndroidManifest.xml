<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Notificaciones (opcional, solo si las usas) -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- (Opcional) Cámara: solo si la usas hoy. Si no, quítalo. -->
    <!-- <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" /> -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CafeRaquelita"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".activities.AuthActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CafeRaquelita"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <activity
            android:name=".activities.InventoryActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.TablesActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.TableProductsActivity"
            android:exported="false"
            android:parentActivityName=".activities.TablesActivity" />

        <activity
            android:name=".activities.BillingActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.CashRegisterActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.InvoiceActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.InvoiceHistoryActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.InvoiceReportActivity"
            android:exported="false"
            android:parentActivityName=".activities.InvoiceHistoryActivity" />

        <!-- Actividades del Dashboard -->
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.FinancialReportsActivity"
            android:exported="false"
            android:parentActivityName=".activities.DashboardActivity" />

        <activity
            android:name=".activities.IncomeDetailsActivity"
            android:exported="false"
            android:parentActivityName=".activities.FinancialReportsActivity" />

        <activity
            android:name=".activities.ExpenseDetailsActivity"
            android:exported="false"
            android:parentActivityName=".activities.FinancialReportsActivity" />

        <activity
            android:name=".activities.ProfitDetailsActivity"
            android:exported="false"
            android:parentActivityName=".activities.FinancialReportsActivity" />

        <!-- FileProvider (único) para compartir PDFs/archivos privados -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
