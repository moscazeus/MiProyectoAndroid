<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/white"
    android:layout_margin="8dp"
    android:elevation="4dp">

    <!-- Header de la mesa -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="12dp"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/textViewTableNumber"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Mesa 1"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/cafe_dark" />

        <TextView
            android:id="@+id/textViewTableStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="LIBRE"
            android:textSize="12sp"
            android:textStyle="bold"
            android:padding="8dp"
            android:background="@color/cafe_green"
            android:textColor="@color/white"
            android:minWidth="60dp"
            android:gravity="center" />

    </LinearLayout>

    <!-- Información del mesero -->
    <TextView
        android:id="@+id/textViewWaiter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mesero: Sin asignar"
        android:textSize="16sp"
        android:textColor="@color/cafe_brown"
        android:layout_marginBottom="8dp"
        android:drawableStart="@android:drawable/ic_menu_myplaces"
        android:drawablePadding="8dp" />

    <!-- Información de productos -->
    <TextView
        android:id="@+id/textViewItems"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Productos: 0"
        android:textSize="16sp"
        android:textColor="@color/cafe_brown"
        android:layout_marginBottom="8dp"
        android:drawableStart="@android:drawable/ic_menu_gallery"
        android:drawablePadding="8dp" />

    <!-- Total -->
    <TextView
        android:id="@+id/textViewTotal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Total: $0.00"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/cafe_green"
        android:layout_marginBottom="16dp"
        android:drawableStart="@android:drawable/ic_menu_recent_history"
        android:drawablePadding="8dp" />

    <!-- Botones de acción mejorados -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <Button
            android:id="@+id/buttonOpenTable"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ABRIR MESA"
            android:layout_marginEnd="8dp"
            android:background="@color/cafe_green"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textSize="12sp"
            android:padding="12dp"
            android:minWidth="100dp"
            android:drawableStart="@android:drawable/ic_menu_add"
            android:drawablePadding="4dp" />

        <Button
            android:id="@+id/buttonAddProduct"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="GESTIONAR"
            android:layout_marginEnd="8dp"
            android:background="@color/cafe_blue"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textSize="12sp"
            android:padding="12dp"
            android:minWidth="100dp"
            android:drawableStart="@android:drawable/ic_menu_edit"
            android:drawablePadding="4dp" />

        <Button
            android:id="@+id/buttonCloseTable"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CERRAR"
            android:background="@color/cafe_red"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textSize="12sp"
            android:padding="12dp"
            android:minWidth="100dp"
            android:drawableStart="@android:drawable/ic_menu_close_clear_cancel"
            android:drawablePadding="4dp" />

    </LinearLayout>

</LinearLayout> 