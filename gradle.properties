org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+UseParallelGC
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.daemon=true

# Kotlin sin daemon: compila en el mismo proceso de Gradle
kotlin.compiler.execution.strategy=in-process
kotlin.daemon.jvm.options=-Xmx2048m,-XX:+UseParallelGC

android.useAndroidX=true
android.enableJetifier=true
android.nonTransitiveRClass=true

org.gradle.configuration-cache=false

android.suppressUnsupportedCompileSdk=36

# (Opcional si tu red es lenta)
# org.gradle.internal.http.connectionTimeout=180000
# org.gradle.internal.http.socketTimeout=180000
